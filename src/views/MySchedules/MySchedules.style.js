const styles = theme => ({
    root: {
        margin: 'auto',
        overflow: 'hidden',
    },
    gridContainer: {
        marginTop: 32,
        marginBottom: 32,
        padding: 16
    }
})

export default styles;